import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  // const linkElement = screen.getByText(/Learn React/);
  // console.log(linkElement.attriubtes)
  // expect(linkElement).toBeInTheDocument();
});
